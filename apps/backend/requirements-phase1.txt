# Phase 1A–1D CV stack
# Install from repo root:
#   pip install -r apps/backend/requirements-phase1.txt
#
# Note: dlib on Windows often needs Visual Studio Build Tools + CMake.
# ultralytics is required for Module 1D inference + training.

numpy>=1.24,<2.0
scipy>=1.11,<1.14
opencv-python==4.10.0.84
mediapipe==0.10.14
dlib==19.24.6
ultralytics==8.2.0
