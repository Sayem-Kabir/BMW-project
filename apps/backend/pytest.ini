[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
pythonpath = .
markers =
    integration: cross-phase ASGI smoke (Module 7A); needs app + mocks, optional DB
filterwarnings =
    ignore::DeprecationWarning
